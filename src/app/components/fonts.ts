import { Amatic_SC, Jua } from "next/font/google";

export const jua = Jua({
    weight: "400",
    display: "swap",
    subsets: ["latin"],
});

export const amaticSC = Amatic_SC({
    weight: ["400", "700"],
    display: "swap",
    subsets: ["latin"],
});
